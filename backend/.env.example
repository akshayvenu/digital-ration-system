# Server
PORT=5000
NODE_ENV=development

# Database
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=admin
DB_NAME=ration_tds

# JWT
JWT_SECRET=4928b18524b08cda59f3fc2e48c0d7b4
JWT_EXPIRES_IN=7d

# Email (Nodemailer)
#
# GMAIL USERS:
# - You CANNOT use your normal Gmail password.
# - You MUST enable 2-Step Verification and then create an App Password.
# - Without 2FA, use a dev sandbox provider instead (see below) or leave blank for mock mode.
#
# ALTERNATIVES (recommended for development):
# - Mailtrap: EMAIL_HOST=sandbox.smtp.mailtrap.io / port 587 / username+password from dashboard
# - Resend / SendGrid: Use their Node SDK instead of raw SMTP (adjust code accordingly)
# - Mock mode: leave EMAIL_USER and EMAIL_PASSWORD empty; emails will be logged to console.
#
# To enable Gmail App Password:
# 1. Turn on 2-Step Verification in Google Account Security.
# 2. Open https://myaccount.google.com/apppasswords
# 3. Generate password (App: Mail, Device: Other) and paste it below.
# 4. Set EMAIL_SECURE=true and PORT=465 for SSL.
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=465
EMAIL_SECURE=true
EMAIL_USER=rationakshay@gmail.com
EMAIL_PASSWORD=fbesoiqdlfmnmwaq
EMAIL_FROM=Ration TDS <rationakshay@gmail.com>

# Verification Code
CODE_EXPIRY_MINUTES=10
CODE_LENGTH=6

